<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ttcs_shoppinng_mall">
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TTCS_shoppinng_mall">
        <activity
            android:name=".ui.activity.ActivityLogin"
            android:exported="true"
            android:launchMode="singleTop"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.Detail"
            >
        </activity>
        <activity android:name=".ui.activity.ActivityMain">

        </activity>
        <activity android:name=".ui.activity.ActivitySingup" />


    </application>

</manifest>